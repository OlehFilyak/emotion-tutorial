/** @jsxImportSource @emotion/react */

function Numbers() {
  return (
    <div
      css={{
        padding: 8,
        zIndex: 200,
      }}
    >
      Тут додано padding 8px і z-index 200. Числа Когда числа являются значением
      свойства css, к числу добавляется px, если только это не свойство css,
      которое не является единицей измерения.
    </div>
  );
}

export default Numbers;
